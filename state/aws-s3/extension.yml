name: state-aws-s3
description: Sync EXT state to AWS S3

config:
  bucket:
    type: string
    description: The S3 Bucket to store state in
  prefix:
    type: string
    default: /
    description: |
      The optional prefix to store state in S3 under