# org: your-serverless-framework-dashboard-org-name (app.serverless.com)
service: new-service

example:
  extension: site@latest
  config:
    src: ./src
    # domain: static-site-extension.com # Add a custom domain connected to an AWS Route53 Hosted Zone
    
